#include <iostream>
using namespace std;


/**
 * Arithmetic Operators
 ** Pluse(+), Minus(-), Divider(/), Asterisk(*), Modulo(%)
 * Relational Operators --> >, >=, <, <=, ==, !=
 * Logical Operators --> || (pipe), &&(Ampersand), ! 
 */

 int main() {

     int a = 5;     
     int b = 2;

     //cout << "Division of a / b is: " << (double)(a / b) << endl;
     //cout << "modulo of a + b is: " << (a % b) << endl; // Return the reminder of the divided value 10 % 8 = 2

     cout << !(1 > 2) << endl;

     return 0;
 }