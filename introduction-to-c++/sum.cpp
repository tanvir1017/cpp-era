#include <iostream>
using namespace std;

int  main(){
    // Declaring the variable and their type;
    double a, b;

    cout << "Please enter first number" << endl;

    // Taking and asiging input from user into variable
    cin >> a;
    cout << "Please enter last number" << endl;
    
    cin >> b;

    cout << "Sum first and last number is = " << (a + b) << endl;
    return 0;
}